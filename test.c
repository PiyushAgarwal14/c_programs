#include <stdio.h>

int main () {
    
    int a[10] = {1,3,2,4,8,6,5};
    int i, n;
    
    printf("Maximum value of array is: \n");
    
    for(i=0; i<=a[i]; i++) {
       
        if(a[i+1]>a[i]) {
            n = a[i+1];
        }
    }
    
    printf("%d", n);
    

    
    return 0;
}
